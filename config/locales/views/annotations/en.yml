en:
  junction:
    views:
      annotations:
        index:
          annotations_tab: Annotations
          description: View known and custom annotation usage across the catalog.
          entity_type_tab: Entity Types
          known: Known annotations
          other: Other annotations
          other_values:
            one: 1 other value
            other: "%{count} other values"
          secret_warning: >-
            Annotation keys and values are listed here in plain text for every
            entity type. Never use annotations for secrets such as tokens,
            passwords, or API keys.
          title: Annotations
        keys:
          empty: No annotations are currently present.
        entity_types:
          empty_entity_types: No entity types are configured.
