# Components must have an owner. Group two has no members, so these components
# are not owned by any signed in user, which the dashboard empty states rely on.
one:
  title: Component One
  name: component-one
  description: MyText
  lifecycle: experimental
  component_type: api
  repository_url: MyString
  image_url: https://example.com/image.png
  owner: two

two:
  title: Component Two
  name: component-two
  description: MyText
  lifecycle: production
  component_type: worker
  repository_url: MyString
  image_url:
  owner: two
