# Components must have an owner. Specs sign in as user one, who is not a member
# of group two, so these components are never owned by the signed in user, which
# the dashboard empty states rely on.
component_one:
  kind: Component
  title: Component One
  name: component-one
  description: MyText
  lifecycle: experimental
  type: api
  image_url: https://example.com/image.png
  owner: group_two
  spec:
    repository_url: MyString

component_two:
  kind: Component
  title: Component Two
  name: component-two
  description: MyText
  lifecycle: experimental
  type: api
  image_url:
  owner: group_two
