# These entities must have an owner. Specs sign in as user one, who is not a
# member of group two, so they are never owned by the signed in user, which the
# dashboard empty states rely on.
system_one:
  kind: System
  title: System One
  name: system-one
  type: service
  description: MyText
  image_url: https://example.com/image.png
  domain:
  owner: group_two

system_two:
  kind: System
  title: System Two
  name: system-two
  type: feature-set
  description: MyText
  image_url:
  domain:
  owner: group_two
